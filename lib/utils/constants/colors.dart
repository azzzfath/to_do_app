import 'package:flutter/material.dart';

class AppColors {
  // App theme colors
  static const Color primary = Color(0xFF5038BC);
  static const Color secondary = Color(0xFFFFFFFF);
  static const Color accent = Color(0xFFebe7ff);

  // Text color
  static const Color text = Color(0xFF000000);
  static const Color textSecondary = Color(0xFFFFFFFF);

  // Button colors
  static const Color buttonPrimary = Color(0xFF5038BC);
  static const Color buttonSecondary = Color(0xFFFFFFFF);
  static const Color buttonAccent = Color(0xFFebe7ff);
}
